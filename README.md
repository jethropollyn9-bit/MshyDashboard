# Challengers FC - Squad Management App

**Live Demo:** [Click here to view the live app!]
file:///Users/jethropollyn/MshyDashboard/index.html

## Overview
Challengers FC is a dynamic, client-side web application built to manage a football club's roster. It features a clean, responsive EA Sports-inspired UI and allows users to dynamically add, manage, and remove players while automatically calculating the overall team rating.

## ✨ Features
* **Full CRUD Functionality:** Users can Create (sign), Read (view), and Delete (sack) players dynamically.
* **Persistent Memory:** Utilizes browser `localStorage` to save the squad state, ensuring data isn't lost on page refresh.
* **Dynamic Math Logic:** Automatically recalculates the Team Average OVR and Squad Size whenever a roster change is made.
* **Fully Responsive:** Custom CSS media queries ensure the dashboard scales perfectly on mobile devices, tablets, and desktop monitors.

## Tech Stack
* **HTML5:** Semantic structure and form inputs.
* **CSS3:** Custom styling, CSS Grid/Flexbox, hover states, and mobile media queries.
* **Vanilla JavaScript (ES6+):** DOM manipulation, event listeners, array methods (`push`, `splice`, `forEach`), and JSON parsing/stringifying.
* **Deployment:** Hosted live via GitHub Pages.

## Learning Outcomes
I built this project from scratch to strengthen my foundational frontend skills. It demonstrates my ability to:
* Manage state and data using JavaScript arrays and objects.
* Connect user inputs to interactive DOM updates.
* Handle browser memory mapping (`localStorage`).
* Use Git version control and deploy live applications.

---
*Created by Jethro Pollyn*